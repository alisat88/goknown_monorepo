// Kept as a compatibility import for code outside the container registration.
// DigitalOcean Spaces implements the S3 API, so it uses the shared provider.
export { default } from './S3StorageProvider';
